<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow plain HTTP to the local Med Lion HR PC server (LAN IPs only). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
